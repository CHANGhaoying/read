<!--components/navbar/navbar.wxml-->
<view class="top" style="height:{{topH}}px">
  <view>
    <span class='icon-fanhui iconfont' bindtap='back' wx:if='{{!flag}}'></span>
    <text>{{text}}</text>
  </view>
</view>
